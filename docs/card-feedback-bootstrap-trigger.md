# Card feedback bootstrap trigger

This temporary marker triggers the already-installed one-time workflow that creates the implementation branch for the failed-card visual feedback fix. The implementation branch removes this marker before merge.
