# ProjectC 分支与提交策略

本文件适用于 ProjectC 项目中的所有 AI 协作对话与 Agent，包括策划、制作、程序、网页原型和未来正式工程。

## 1. 默认原则

不要仅因为修改发生在 GitHub 仓库中，或修改对象是文档，就自动创建新分支。

分支的用途是隔离尚未确认、高风险、可能冲突或需要独立审查的变更；它不是所有仓库写入的必经步骤。

## 2. 默认直接提交 `main`

以下变更在用户已经明确确认内容和范围时，默认直接提交到 `main`：

- 纯文档的计划落实、结论同步和状态更新；
- `task-log`、`sync-status`、Changelog 等进度记录；
- 小范围措辞、格式和交叉引用修正；
- 已经讨论并接受的设计结论写入现有文档；
- 不改变代码、运行行为、ruleset、Schema 或配置语义的维护工作。

直接提交前必须：

1. 读取目标文件在 `main` 上的最新版本和 SHA；
2. 确认只修改本次任务范围内的文件；
3. 避免覆盖其他 Agent 或用户刚提交的内容；
4. 使用清晰、单一目的的提交说明；
5. 完成后报告实际提交 SHA 和修改范围。

不得强制改写 `main` 历史。

## 3. 默认使用新分支和 Pull Request

以下情况默认建立独立分支并通过 PR 审查：

- 修改程序代码、运行时行为、场景、资源或构建流程；
- 修改配置语义、Schema、rulesetVersion 或可执行规则；
- 尚未获得用户确认的设计重构；
- 大规模删除、归档、迁移或重新组织历史内容；
- 跨仓库且会改变实现、行为或版本对应关系的协同变更；
- 可能与其他正在进行的工作发生冲突；
- 需要 A/B 对照、独立回滚或明确审查节点；
- 用户明确要求创建分支、PR 或先提供 Diff 审查。

制作与程序任务只要涉及实现行为，默认属于这一类；不能因为改动规模较小就直接写入 `main`。

## 4. 冲突与不确定性

准备直接提交 `main` 时，如果发现：

- 文件在读取后已经发生变化；
- 修改范围混入无关内容；
- 用户确认不足；
- 无法判断是否会影响运行行为；
- 存在并行分支或正在审查的相关 PR；

则停止直接写入，改用独立分支和 PR，或先向用户说明冲突。

## 5. 跨对话一致性

策划、制作和程序对话使用同一套规则：

- 已确认的纯文档维护优先直接提交 `main`；
- 未确认或涉及实现行为的工作优先使用分支和 PR；
- 用户的明确指令始终高于默认策略。

该策略适用于 ProjectC 文档仓库、ProjectC-WebPrototype，以及未来建立的正式工程仓库。